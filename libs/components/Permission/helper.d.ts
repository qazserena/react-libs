import { PermissionTree } from '../../index';
/**
 * 检查是否有权限
 * @param permissionTree
 * @param path
 * @returns
 */
export declare function checkPermission(permissionTree: PermissionTree, path: string | string[]): boolean;
